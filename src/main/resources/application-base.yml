spring:
  application:
    name: ordering
  jpa:
    hibernate:
      ddl-auto: validate
      show-sql: false
  flyway:
    enabled: false
    url: jdbc:postgresql://localhost:5432/ordering
    locations: classpath:db/migration

algashop:
  integrations:
    shipping:
      provider: RAPIDEX